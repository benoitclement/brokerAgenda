@model IEnumerable<Broker>

@{
  ViewData["Title"] = "liste des courtiers";
  string modifType = null;
  if (TempData["modification"] != null) modifType = TempData["modification"].ToString();
  int? modifiedId = null;
  if (TempData["modifiedId"] != null)
  {
    modifiedId = Convert.ToInt32(TempData["modifiedId"]);
  }
}
  
<div class="container p-3">
  <div class="d-flex justify-content-between mb-3">
    <h2 class="text-primary">Liste des courtiers</h2>
    <a class="btn btn-primary col-3" asp-area="" asp-controller="Broker" asp-action="Create">Ajouter un nouveau courtier</a>
  </div>
  @if (Model.Count() > 0)
  {
    <table class="table table-bordered table-striped" style="width:100%">
      <thead>
        <tr>
          <th>Id
            <a asp-controller="Broker" asp-action="Index" asp-route-SortOrder="Id" class="ml-2"><img style="width: 15px" class="" src="~/images/sort-descending.svg" alt="sort descending icon" title="sort descending icon" /></a>
          <th>Nom de famille
            <a asp-controller="Broker" asp-action="Index" asp-route-SortOrder="LastName" class="ml-2"><img style="width: 15px" class="" src="~/images/sort-descending.svg" alt="sort descending icon" title="sort descending icon" /></a>
          </th>
          <th>Prénom
            <a asp-controller="Broker" asp-action="Index" asp-route-SortOrder="FirstName" class="ml-2"><img style="width: 15px" class="" src="~/images/sort-descending.svg" alt="sort descending icon" title="sort descending icon" /></a>
          </th>
          <th>Modifications</th>
        </tr>
      </thead>
      <tbody>
        @foreach (var broker in Model)
        {
          string trClass = "";
          if (modifiedId != null && modifiedId == broker.Id)
          {
            if (modifType == "create") trClass = "created-entry";
            else if (modifType == "update") trClass = "updated-entry";
          }
          <tr class="@(trClass)">
            <td width="20%">@broker.Id</td>
            <td width="20%">@broker.Lastname</td>
            <td width="20%">@broker.Firstname</td>
            <td width="40%">
              <div class="col-6 d-inline-flex align-content-between w-100">
                <a asp-controller="Broker" asp-action="Details" asp-route-Id="@broker.Id" class="btn btn-info d-inline-flex mx-1"><img style="width: 20px" class="mr-1" src="~/images/info-circle-solid.svg" alt="update icon" title="update icon" />Profil</a>
                <a asp-controller="Broker" asp-action="Update" asp-route-Id="@broker.Id" class="btn btn-primary d-inline-flex mx-1"><img style="width: 20px" class="mr-1" src="~/images/edit-solid.svg" alt="update icon" title="update icon" />Modifier</a>
                <a asp-controller="Broker" asp-action="Delete" asp-route-Id="@broker.Id" class="btn btn-danger d-inline-flex mx-1"><img style="width: 20px" class="mr-1" src="~/images/trash-solid.svg" alt="delete icon" title="delete icon" />Supprimer</a>
              </div>
            </td>
          </tr>
        }
      </tbody>
    </table>
  }
</div>
@{
  if (modifType != null)
  {
    string action = "";
    if (modifType == "create") action = "créé";
    else if (modifType == "update") action = "modifié";
    else if (modifType == "delete") action = "supprimé";
    <div class="modification-container">
      <div class="modification-details @TempData["modification"]">
        <p>Le courtier <strong>@TempData["brokerName"]</strong> a bien été @action.</p>
      </div>
    </div>
  }
}
